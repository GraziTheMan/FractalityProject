# Python dependencies for the Fractality API (api/).
#
# Separate from consciousness_backend/requirements.txt on purpose: this is the
# deployable web service and stays lean, while the consciousness research layer
# pulls in numpy/torch-scale dependencies that would bloat this image.
#
#     pip install -r api/requirements.txt
#     uvicorn api.main:app --reload --port 8000

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
neo4j>=5.20
PyJWT[crypto]>=2.8      # Clerk session token verification
httpx>=0.27             # JWKS fetch

# dev / test
pytest>=8.2
pytest-asyncio>=0.23
